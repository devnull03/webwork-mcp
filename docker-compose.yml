services:
  webwork-mcp:
    build: .
    env_file: .env
    environment:
      - PORT=3000
      - POKE_NAME=${POKE_NAME:-Webwork MCP}
    ports:
      - "3000:3000"
    volumes:
      - ./.env:/app/.env
      - ~/.config/poke:/root/.config/poke
